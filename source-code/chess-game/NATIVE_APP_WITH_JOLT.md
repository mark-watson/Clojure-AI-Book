# Use Jolt to built native apps in Clojure

Docs:

https://github.com/jolt-lang/jolt

https://jolt-lang.github.io

On macOS, you can just install jolt with:

```
brew install jolt-lang/jolt/jolt
```

Try building the Chess game:

```
 jolt build -m chess-game.cli -o chess_jolt
 ./chess_jolt
```
